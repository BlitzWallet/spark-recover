import "./style.css";
export default function ActivityIndicator({ width, height }) {
  return (
    <div
      style={{
        width,
        height,
      }}
      id="activityIndicator"
    />
  );
}
